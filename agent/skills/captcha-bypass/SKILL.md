---
name: captcha-bypass
description: 验证码绕过——短信轰炸/图形验证码/逻辑缺陷。Use when 看到验证码/短信发送/注册登录爆破点。
---

# 验证码绕过

> 蒸馏自CC挖洞武器库 captcha-bypass-完整手册（2026实战验证）

## 短信轰炸（最易出洞）

```bash
# 找短信发送接口（注册/登录/找回密码）
# 绕过手法：
# 1. 参数篡改：手机号加空格/双写/+86/unicode
#   13800138000 → " 13800138000" / "13800138000 " / "+8613800138000" / 1380013800 0
# 2. smsType遍历：不同业务类型共享验证码计数
#   smsType=1 → smsType=2/3/4...（每种都发一次）
# 3. 响应包重放：拦截发送请求 → 无限重放
# 4. 订单ID重置：下单→支付验证码→改订单ID再触发
# 5. 未绑定手机号：目标手机号未注册时验证码逻辑缺陷
# 验证：目标手机号短时间收到N条短信 = 实锤
```

## 图形验证码绕过

```bash
# 1. 验证码可重复使用（不过期/不绑定会话）
# 2. 万能验证码：000000/888888/123456 或空验证码
# 3. 验证码返回在响应里（调试接口/JS注释）
# 4. 图形码识别：ddddocr（Python，本地可跑）
pip install ddddocr
python3 -c "import ddddocr; print(ddddocr.DdddOcr().classification(open('cap.png','rb').read()))"
# 5. 验证码不刷新：同一张图反复用
```

## 逻辑缺陷

```bash
# 1. 验证码绑定手机号≠当前手机号（A的验证码B能用）
# 2. 验证码长度/格式校验缺失（任意6位数字）
# 3. 验证码校验接口和发送接口分离（直接调校验）
# 4. 验证码在Cookie/响应头/JS变量里
# 5. 次数限制绕过：X-Forwarded-For轮换/IP参数/清Cookie
```

## 提交要点

- 短信轰炸 = 中危（补天¥500-2000），需目标手机号收到多条短信证据
- 验证码绕过导致撞库/爆破 = 中危
- 纯图形码可识别 = 低危（常不收，需结合爆破场景）
