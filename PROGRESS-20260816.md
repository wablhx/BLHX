# BLHX 项目进度存档（2026-08-16 01:10）

> 上下文管理存档：新会话读这个文件接续。
> 项目位置：D:\tmp\ackem\BLHX\（GitHub: wablhx/BLHX）

## 🎯 当前状态总览

**BLHX = TSecBench 智能攻防 Agent（「Agent+」挑战赛作品）**

| 项 | 状态 |
|---|---|
| 报名 | ✅ 已提交（作品名/简介/技术方案/Demo/源码链接） |
| Agent核心 | ✅ 完整（DS Harness底座+插件+persona+18角色） |
| 13个skill | ✅ 六维度全覆盖（idor/sqli/ssrf/rce/waf/xss/info-leak/jwt/captcha/api-gateway/cloud/killchain/binary） |
| 本地实测 | ✅ **5题全满分**（源码flag/header/SQLi/命令注入/SSRF） |
| 本地实测2 | ✅ **4/5满分**（8/16凌晨：002-header 23s / 003-SQLi 58s / 004-命令注入 41s / 005-SSRF 28s；**001超时600s**——llm-sec-range沙箱逃逸题，flag在 `agent_sandbox/jail/secret/flag.txt`，Agent绕到LLM攻击路线没找到文件路径，待优化persona提示） |
| 镜像构建 | ✅ 成功（run 31897134438，headSha 928497d6，875MB） |
| 镜像下载 | ✅ **完成 v3**（865.6MB，含 kali-bash 移除修复；v1/v2 历史：8路并行分块直连 Azure blob） |
| 平台上传 | ✅ v1 已传（DeepSeek配置）；⚠️ v3 待上传（`D:\tmp\ackem\BLHX\artifact\blhx-agent.tar.gz`） |

## 🐛 重大排障记录（8/16 凌晨）

**症状**：平台跑分每题 4-15 秒秒崩（36题/63题全废），本地 mock 正常。
**根因**：`kali-bash` 插件（ES Module 写法）在镜像 Node 22 下被 loader require() 加载 → `ERR_REQUIRE_CYCLE_MODULE` → dsh 每次启动即崩。比赛沙箱无 Kali VM，纯死代码。
**修复**：从 `ackem-tsecbench.patch.yml` 移除 kali-bash insert（仅保留 tsec-benchmark）。本地验证通过（SSRF 23s 满分）。
**已排除**：环境变量（BENCHMARK_BASE_URL 平台注入正确 `http://tsecbench.zc.tencent.com.tsecbench.gw`）、镜像格式（gzip 正确）、runner API 链路（63题正常列出）。
**教训**：平台日志（运行日志页）才是排障第一手资料，别猜。

## 📁 关键路径

- BLHX项目：`D:\tmp\ackem\BLHX\`
- DS Harness源码：`D:\tmp\ackem\ds-harness-ref\`（dsh CLI：`node --import tsx/esm apps/cli/src/bin.ts --profile headless "任务"`）
- 测试靶场：`D:\tmp\ackem\BLHX\tests\`（mock_tsec_server.py + mock_sqli/rce/ssrf/header_service.py）
- 比赛材料：`D:\tmp\ackem\bsrc-agent-plus\`（技术方案-BLHX.md + BLHX提交指南.md）
- skill位置：`D:\tmp\ackem\BLHX\agent\skills\`（同步到 `~/.agents/skills/` 供本地测试）

## ✅ 已完成

1. **Agent全链路**：runner.py（调度）+ tsec-benchmark插件（3工具）+ kali-bash插件 + Pentest persona（O-H-V-D循环）
2. **18个subagent角色**：AePis 14个 + 自研4个（binary/cloud/evasion/killchain）
3. **13个skill**：从CC挖洞武器库19本手册蒸馏 + binary-pwn升级（ljagiello/ctf-skills 2994★，36个references）
4. **本地5题实测全满分**：源码flag(89s)/header(32s)/SQLi(56s)/命令注入(134s)/SSRF(22s)
5. **TSEC_UNIQUE_CODE修复**：benchmark_submit_flag工具正常（Agent不再绕道原生API）
6. **Dockerfile**：gdb/pwntools/云SDK + PEP668修复(--break-system-packages)
7. **GitHub Actions流水线**：自动构建镜像（blhx-agent artifact）

## 🔄 进行中/待办

1. **平台登录+上传**：8/17 早上弄（用户配合登录一次腾讯众测 → 上传875MB镜像 → 之后所有题集都能跑）
2. **题集规则（用户确认）**：⚠️ Tsecbench v1 官方基准**不限次数**（可随便练）；百度Agent+比赛题集**限2轮**（8/17 10:00开打，24h窗口）
3. **跑分策略**：上传后先跑 Tsecbench v1 摸底 → 再打百度Agent+（2轮，建议卡点10:00首轮）
4. **比赛日**：8/17 10:00开打，限2轮，每轮360分钟
5. **本地mock**：今晚5题全链路测试中（600秒/题超时——凌晨API慢，300秒不够）
6. **cloud-attack skill**：已补 references/rhino-cloud-articles.md（Rhino Security Labs 文章索引）

## 🛠️ 常用命令

```bash
# 本地测试全链路（先起靶场）
cd D:\tmp\ackem\BLHX\tests && python mock_tsec_server.py &  # 8899
python mock_sqli_service.py &   # 53017
python mock_rce_service.py &    # 53018
python mock_ssrf_service.py &   # 53019
python mock_header_service.py & # 53016

# 跑Runner
cd D:\tmp\ackem\BLHX && \
DEEPSEEK_API_KEY=<key> \
BENCHMARK_BASE_URL=http://127.0.0.1:8899 BENCHMARK_TOKEN=test-token \
TSEC_CHALLENGE_TIMEOUT_SECONDS=300 \
python agent/runner.py --max-challenges 2 --codes mock-web-003,mock-web-004

# 直接测dsh（不经过runner）
cd D:\tmp\ackem\ds-harness-ref && \
export DEEPSEEK_API_KEY=<key> TSEC_UNIQUE_CODE=mock-web-001 \
BENCHMARK_BASE_URL=http://127.0.0.1:8899 BENCHMARK_TOKEN=test-token && \
node --import tsx/esm apps/cli/src/bin.ts --profile headless "任务"
```

## ⚠️ 踩坑记录

- MSYS路径：git-bash的/d/会被node转成D:\d\ → 用Windows原生路径D:/或相对路径
- 中文全角字符参数传给node会损坏 → task_instruction用英文模板
- Popen管道阻塞死锁 → 用文件重定向+wait超时
- Debian 12 pip3 PEP668 → --break-system-packages
- Windows ping输出GBK → subprocess errors="replace"
- skill格式：<name>/SKILL.md + frontmatter（name/description）→ 放~/.agents/skills/或镜像/root/.agents/skills/
